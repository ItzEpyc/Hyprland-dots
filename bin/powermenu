#!/bin/env bash

choice=$(printf "Lock\nLogout\nReboot\nShutdown" | rofi -m -1 -dmenu)
case "$choice" in
  Lock) hyprlock ;;
  Logout) pkill -KILL -u "$USER" ;;
  Reboot) systemctl reboot ;;
  Shutdown) systemctl poweroff ;;
esac
